# Rule-based-logic
This project is a Spam Email Classifier built using machine learning. It uses TF-IDF Vectorization to convert text messages into numerical form and applies the Naive Bayes algorithm to classify messages as Spam or Ham (Not Spam). The model is trained on labeled data and can accurately predict whether a new message is spam.
